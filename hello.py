# CloudUp string
def lambda(context, context_lambda, context_params, memory_id):
    context = context
    context_lambda = context_lambda
    context_params = context_params
    memory_id = memory_id
    lambda = 'Hello World!'
    params = 'secure this lambda application!'
